# install eslint vscode extension as dev dependency
npm install --save-dev eslint

# initialize eslint with powershell
npx eslint --init

# install @eslint/create-config as dev dependency
npm install --save-dev @eslint/eslint-config